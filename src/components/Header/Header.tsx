import styles from "./Header.module.css";

const Header = () => {
  return (
    <header className={styles.header}>
      <h1 style={styles}>Sweet Scoops Gelato</h1>
    </header>
  );
};

export default Header;
